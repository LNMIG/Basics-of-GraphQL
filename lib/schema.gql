type Query {
    "Devuelve todos los cursos"
    getCourses: [Course]
    "Devuelve un curso"
    getCourse(id: ID!): Course
}

type Course {
    _id: ID!
    title: String!
    teacher: String
    description: String!
    topic: String
}